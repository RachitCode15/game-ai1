from typing import Dict


def analyze_clip(filepath: str) -> Dict:
    """Dummy clip analyzer (replace with CV/ML later)."""
    return {
        "kills": 5,
        "accuracy": "70%",
        "movement": "aggressive",
        "filepath": filepath,
}